# nucleagent-executor web（子应用）本地开发环境变量
# 复制为 .env.local 后按需修改。

# dev server 端口（默认 26698；micro-app 壳应用编排时可注入）
EXECUTOR_WEB_PORT=26698

# /api 代理目标（executor 后端 prism-fusion，默认 http://localhost:26690）
EXECUTOR_BACKEND_URL=http://localhost:26690
